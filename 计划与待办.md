# IELTS Beach · 计划与待办（期末后开工）

> 状态：📋 **方案已定稿，暂不动手**。等期末周结束后（按《暑期执行计划》在 7/26 全力期）开工。
> 这是单一入口：你只要打开这一个文件，就能找到所有 ielts 相关的事和它们的详细文档。

---

## 0. 相关文档导航
| 文档 | 内容 |
|------|------|
| **本文件** | ielts 所有待办的总清单 + 开工顺序 |
| `ielts-beach改造方案.md` | 像素风重做 + 大词库的**详细技术方案** |
| `../暑期执行计划.md` | 7/26–8/9 全力期的逐日安排（ielts 在第 3 节）|
| `../成长计划.md` | ielts 作为"目标 B·第一个作品"的定位 |

---

## 1. 两大改造（详见 `ielts-beach改造方案.md`）

### 🎨 像素风重做（纯 CSS·小像素格）
- [ ] 落地 `--px` 像素网格 + `pixel-art.js` 生成器（字符画 → box-shadow）
- [ ] 画像素图谱：太阳 / 月亮 / 云 / 海浪 / 沙滩 / 棕榈 / 海鸥 / 星空
- [ ] 大面积背景用 2 色像素抖动（保性能、保轻量）
- [ ] 动画全切 `steps()` 跳帧；重定义离散调色板（日/夜）
- [ ] 真机性能检查 + 降级开关

### 📚 大词库（综合三方案·够大且轻量）
- [ ] 改造 `scripts/build-words.js`：词单 → AI 补全 → 校验去重 → 分片 + manifest
- [ ] 新增 `public/js/word-source.js`：WordSource 抽象 + 静态分片实现 + 词典 API 兜底
- [ ] 改 `public/js/words.js`：依赖 WordSource + IndexedDB 缓存懒加载
- [ ] 生成 2000–3000 词分片并托管
- [ ] 预留 D1 后端升级接口

---

## 2. 上线 Checklist（让网站真正"能用")

> 来自全力期逐日表，按顺序做，第一项最快出成果。

- [ ] **B1** 部署后端：`wrangler login` → `wrangler deploy` → `wrangler d1 migrations apply`
- [ ] **B2** 前后端联通：跑通注册/登录/同步，补 `worker/src/auth.js` 的 `hashPin()`
- [ ] **B3** 接入大词库（见上"大词库"改造）
- [ ] **B4** 接 Open-Meteo 天气 API（`public/js/weather.js` 的 `fetchWeather()`）
- [ ] **B5** 修 Day Streak 统计 / 收藏云端同步 / PIN 哈希安全
- [ ] **B6** 前端上线（Vercel / Cloudflare Pages）+ 写 README/部署文档
- [ ] **B7** 上线后每天用它背词（吃自己狗粮 + 收集真实反馈）

---

## 3. 开工前要定的 3 个小决策（想好了填这里）

1. 分片托管在哪：☐ GitHub raw　☐ Cloudflare Pages/R2 → 决定：______
2. 词库目标规模：☐ 2000　☐ 3000 词 → 决定：______
3. 词典 API 兜底：☐ 默认开　☐ 关 → 决定：______

---

## 4. 建议的开工顺序（期末后第一天起）

1. **先 B1 部署后端**（半天、立竿见影、最有成就感）→ 网站第一次真正"活"起来
2. 再做 **大词库**（B3，工程量最大，趁状态好啃）
3. 然后 **像素风**（独立、纯前端，做累了拿它换换脑子）
4. 最后 **B4–B7 收尾上线**

> ✅ 现在你什么都不用做，安心考试。期末一结束，从第 4 节第 1 步开始即可。

---

## 5. 已知 Bug（待修复）

### 🐛 游戏分数显示错误
**症状**：背词过程中，右侧"不认识"数字最多显示 1，标记"认识"后数字清零。进度条也不正确。
**位置**：`public/js/game.js` — `_refreshGameHeader()` 及相关分数更新逻辑
**状态**：📋 待修复（2026/7/3 记录）
**排查方向**：store 嵌套状态的 get/set 可能存在闭包或异步问题，需从头追踪分数更新的完整链路。
